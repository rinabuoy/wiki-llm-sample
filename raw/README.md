# raw/

Immutable source documents (articles, papers, transcripts, etc.). Drop new sources here; nothing in this directory should ever be edited or deleted by the agent. `assets/` holds locally downloaded images referenced by sources.
